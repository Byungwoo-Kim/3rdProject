<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.pc_22.coupon2">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MapActivity" />

        <meta-data
            android:name="com.kakao.sdk.AppKey"
            android:value="c3b68a30244593e3fb3e819ccf1e5dc1" />

        <activity
            android:name=".giftcouponActivity"/>
        <activity
            android:name=".TempActivity"/>
        <activity android:name=".sepActivity"></activity>
        <activity android:name=".memberjoinActivity"></activity>
        <activity android:name=".CouponPopupActivity"></activity>
    </application>

</manifest>